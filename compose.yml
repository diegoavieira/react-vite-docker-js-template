services:
  app:
    container_name: reactvitedockerjstemplate
    build: .
    volumes:
      - .:/app
    ports:
      - 5173:5173
    command: sh -c "rm -rf /app/node_modules && cp -rfu /cache/node_modules/. /app/node_modules/ && exec npm run dev"
    environment:
      NODE_ENV: development
    tty: true
    stdin_open: true
